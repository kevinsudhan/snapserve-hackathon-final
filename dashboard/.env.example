# Point the dev proxy at the backend (default http://localhost:8000)
VITE_BACKEND_URL=http://localhost:8000

# Set to 1 to run the whole dashboard on realistic in-browser mock data
# (every REST endpoint + a synthetic WebSocket event stream). No backend needed.
VITE_MOCK=0
